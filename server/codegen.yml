overwrite: true
schema: "http://localhost:4000/graphql/"
documents: null
generates:
  src/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      declarationKind: 'interface'
